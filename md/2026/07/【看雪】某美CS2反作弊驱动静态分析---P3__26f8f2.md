---
title: 【看雪】某美CS2反作弊驱动静态分析 - P3
source: https://bbs.kanxue.com/thread-292165.htm
source_host: bbs.kanxue.com
clip_date: 2026-07-25T19:05:22+08:00
trace_id: 46af17e6-d1c7-426d-9005-3fd50729ff4e
content_hash: 8d851d694cc7f18aae1f36c84aa4c89d0e3da9231ee17da11431523aacc717bf
status: summarized
tags:
  - 看雪
  - Windows逆向
  - 内核
series: null
feed_source: 看雪·逆向工程
ai_summary: 某美CS2反作弊驱动通过Intel VT虚拟化技术实现内存页监控和虚拟化环境检测，以捕获作弊行为。
ai_summary_style: key-points
images_status:
  total: 62
  succeeded: 31
  failed_urls:
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
    - src
notion_page_id: 3a875244-d011-813d-baf3-f17ac3ec6e67
ioc: null
---

> 💡 **AI 总结（key-points）**
>
> 某美CS2反作弊驱动通过Intel VT虚拟化技术实现内存页监控和虚拟化环境检测，以捕获作弊行为。
> 
> - **虚拟化环境检测**：驱动使用CPUID、CR0/CR3/CR4、MSR、XSETBV等寄存器测试来检测虚拟机环境、内核patch和调试器，返回位掩码标识异常。
> - **VT初始化与EPT监控**：用户态通过XOR加密消息触发驱动初始化VT，建立EPT监控指定进程的内存页，支持大范围和单页两种模式，请求结束时退出VT。
> - **EPT违规处理与栈回溯**：当内存页访问触发EPT违规时，驱动通过DPC和APC机制在命中线程上下文中执行栈回溯，收集内核和用户态调用链。
> - **上报机制与应用场景**：检测结果通过XOR加密输出包返回用户态，可用于捕获外部程序读写、内核驱动访问或DLL注入的异常行为。

## 四．基于VT虚拟化的内存页监控和无模块驱动检测

### 4.1 虚拟化环境检测

在我往下挖的时候就发现了一个VT驱动检测点sub_1400EA90E  
![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/7aba73ad2ef14bb2.webp)

还原完成就是这样（自行放大看哈）

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/0b53c9414b5a9c8e.webp)

简单来说，这就是一个环境检测函数，他会检测你的环境是不是看着很可疑,例如：

疑似被驱动抢占虚拟化

```
疑似虚拟机环境
```

```
疑似内核 patch
```

并进行置位，返回值是bitmask.

如果异常的话，那么就会使用切到更细的 VAD/section 枚举路径对你的环境进行分析，也就是一个进程一个进程的扫你的VAD树（可能是出于性能考虑才在环境异常后干这个而不是常态干），那么到底test了一些什么呢？！

·CPUID.1:ECX\[31\] Hypervisor Present（如果这个位是1，那么就表示在hypervisor 下面跑）

·读Hyper-V / 虚拟化 synthetic MSR的这一段0x40000000 ~ 0x400000FF（正常裸机读这些 MSR 大概率返回 #GP，如果能读到东西说明大概率是虚拟机或者有虚拟化层）

·看CPU的APERF/MPERF支持和MSR实际读取是否一致（比如CPUID说我不支持，结果MSR 0xE7 / 0xE8读了又不#GP，那肯定有问题）

·尝试翻转CR0.NE的值，如果写入失败了那么就肯定是被拦了,返回true环境该位异常

·对 CR0 做一些很逆天的测试，比如尝试往 CR0 的高 32 位 reserved bit 里面写 1。正常 CPU 对这些保留位是很严格的，你乱写肯定要 #GP；如果写进去了，或者写完读回来还像真的一样，那说明这层 CPU 状态大概率是被 hypervisor 模拟出来的，而且模拟得还不够像

·测试 CR0 的低位保留位，比如构造一些正常系统根本不该接受的 CR0 组合。正常裸机会直接异常，如果你的 VMM/虚拟化层为了省事直接把 mov cr0, xxx 放过去了，那这个 bit 就会被打上异常分

·测 CR3，它会先用 CPUID 0x80000008 拿到 CPU 的物理地址宽度，比如 48 位，然后尝试往 CR3 物理地址宽度之外的 bit 里面塞东西。正常这些 bit 都是 reserved，不能随便写；如果写了不炸，那就是 CR3 虚拟化没处理干净

·测 CR3 bit63，也就是 PCID 下的 no-flush 位。这个位不是任何情况下都能用，只有 CR4.PCIDE 开的时候才有意义。所以它会分别测试“开 PCID”和“没开 PCID”两种情况。如果没开 PCID 的时候 CR3 bit63 还能被接受，那就很假

·还有 CR4.VMXE，也就是 VT-x 开关位。正常来说这个位能不能写、写了能不能读回来、清了以后 VMX 指令是什么异常，都有固定行为。如果你的驱动已经抢占 VT，然后为了隐藏自己对 CR4.VMXE 做了虚拟化，这里就非常容易露馅

·还有 CR4 的非法组合，比如乱改 PAE / LA57 / VMXE 这类关键位。正常 CPU 对 CR4 的组合限制很严格，不能你想怎么写就怎么写。如果 VMM 没按 Intel 手册模拟，就会出现“本来该 #GP 的没 #GP”或者“写完读回来不一致”的情况

·然后就是 CR4 高 32 位 reserved bit，这个和 CR0 高位测试类似，就是往不该写的高位写 1。正常应该直接 #GP，如果没有异常或者状态被错误保留，就说明环境可疑

·还有 XSETBV，它会故意用一个非法的 XCR index，比如 0x45，去执行 xsetbv。正常情况下这个 index 根本不存在，应该异常；如果居然执行成功，那就是 XSETBV 被虚拟化层错误放行了

·还会通过 CPUID 0xD 拿到当前 CPU 支持哪些 XCR0 bit，然后专门找“不支持的 bit”去 xsetbv。正常 CPU 不支持的 XCR0 bit 写进去肯定炸；如果没炸，说明 CPUID 和 XSETBV 行为对不上

·它还测 XCR0 的非法组合，比如：

x87 被关掉、只开 AVX 不开 SSE、AVX512 的几个状态位没一起开、MPX 的 BNDREGS/BNDCSR 不匹配。

这些组合正常都是非法的。如果 hypervisor 对 xsetbv 只是简单保存寄存器值，没有严格校验，就会被这个检测抓出来

·甚至还有 RDTSC 计时检测。它会连续执行两次 rdtsc，中间用 lfence 做序列化，然后看两次的 delta。正常裸机这个差值应该很小；如果因为 VM-exit、TSC offset、TSC scaling 或者调试器导致延迟很大，就会被认为环境异常

·它还会用 KeIpiGenericCall 把检测函数打到所有 CPU 核心上跑，让每个核心都测 rdtsc 是否单调递增。如果某个核心上出现第二次时间戳比第一次还小，那说明 TSC 同步/虚拟化有问题，直接给异常分

·然后测性能计数器，主要是 IA32_FIXED_CTR_CTRL 0x38D、IA32_PERF_GLOBAL_CTRL 0x38F 和 IA32_FIXED_CTR2 0x30B。它会打开 fixed counter，然后连续读计数器，看计数器有没有正常递增、读 MSR 的延迟是不是离谱。如果性能计数器被 VMM 模拟得不准，也会中招

·接着测 MPERF / APERF 的计时行为，也就是 MSR 0xE7 / 0xE8。前面已经测了“CPUID 说支不支持”，这里进一步测“读出来的值增长是否合理”。如果增长太慢、太快、不增长、倒退，或者读 MSR 延迟异常，就说明环境不像真机

·还有一个比较狠的是 cache / MTRR 行为检测。它会先正常测一段内存访问速度，然后设置 CR0.CD 关闭缓存，再改 IA32_MTRR_DEF_TYPE 0x2FF，配合 wbinvd 刷缓存，再测一次内存访问速度。正常关 cache 以后访问速度应该明显变慢；如果没慢多少，说明 cache/MTRR 行为可能被虚拟化层糊弄过去了

·主函数里还直接测了 DR7。它会保存原来的 DR7，然后写入 0x4FF，再读回来。如果读出来不是 0x4FF，说明调试寄存器被拦截、过滤或者虚拟化了。这个一般用来抓调试器、硬件断点隐藏、或者 hypervisor 对 DR 寄存器的处理不一致

·还有两个明确针对 VT 的。第一个是清掉 CR4.VMXE 后执行 VMXON。正常情况下 CR4.VMXE = 0 时执行 VMX 指令应该触发 #UD，也就是非法指令异常。如果它不是 #UD，或者居然执行成功，那基本就是 VMX 指令被你的 hypervisor 接管后没模拟对

·第二个更细，它会先读 IA32_FEATURE_CONTROL 0x3A，确认 VMX 是否被 BIOS/系统允许，然后根据 IA32_VMX_CR0_FIXED0/FIXED1 和 IA32_VMX_CR4_FIXED0/FIXED1 修正 CR0/CR4，最后拿一个故意构造坏的 VMXON region 去执行 VMXON。正常 CPU 应该返回固定的 VMfail 行为；如果你的 hypervisor 返回的 CF/ZF、异常类型、失败路径和真 CPU 不一致，这里就直接露馅

总而言之就是这个某美用了一堆方法，来测试你的机器到底是不是直接交互真实CPU，如果不是预期行为就会给那个位打一个可疑

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/966e0bc337df2808.webp)

### 4.2 某美驱动Intel VT初始化全流程分析

前面环境检测部分提到，这个驱动会主动测试系统当前的虚拟化状态；但是“检测别人有没有抢 VT”和“它自己什么时候启动 VT”是两件事。本节继续从真正的通信入口往下追，看看用户态到底怎样把一个目标进程、一个虚拟地址和一组控制参数交给驱动，驱动又怎样把它们变成 EPT 权限控制，最后执行 VMLAUNCH。

在真正的消息处理函数 sub_14005B5DB 里可以看到，用户态不是直接把明文结构传入驱动，而是通过 \\PacDomain 发送一段 XOR 加密的数据。驱动先解密输入包，再取出输入长度和输出长度，最终调用：

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/2b6c366128dd6f4c.webp)

这就是第一层命令分发。

sub_140053703 一开始取的是：

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/2f6f1686078734a3.webp)

也就是输入包的第二个 DWORD。这个函数本身被控制流混淆得比较厉害，间接调用目标也经过常量还原；不过和这条 VT 请求链相关的两个目标可以直接算出来：

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/9028a19286619e21.webp)

所以完整的外层流程是：

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/1cce53689e615e7f.webp)

sub_140065895 并不就直接是“VT 初始化函数”。它是一个通用二级 RPC 分发器，里面确实有两条会进入 SHV/VT 路径的请求，但另外一些分支只是查询进程、线程、模块、PE 导出或虚拟内存信息。所以必须把这两类行为分开看。

#### 4.2.1 sub_140065895：映射用户缓冲区后再分发

这个二级分发器并不会直接把最初用户态传递的ptr直接丢给handler，而是先调用sub_140064930 进行：

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/b2136bcb95ab21e1.webp)

也就是说，后续代码用的 input / output 已经是锁页后的内核映射地址。请求结束后，sub_140064B73 会对应地解锁页面并释放 MDL，如果锁页失败则会直接ret。

映射成功后，第二层分发器才校验包头：

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/9fd43ae2bb49a87e.webp)

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/29a639686000e815.webp)

第一个 DWORD 是包内声明长度，input + 4 才是二级命令号。如果输入为空、长度不一致，或者非空输出包的长度不一致，这个func会直接返回 0xC000000D；前面映射器失败则返回 0xC0000055。

还有一个很关键的协议细节：这个函数的 C 返回值和 handler 的实际结果是两条不同通道：

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/6e49bab31f0d0bd4.webp)

因此，只要映射和包头校验通过，sub_140065895 函数本身一般都返回 0；具体的业务操作成功还是失败，要看 output + 0x220。这个 64 位结果也不总是标准 NTSTATUS：部分分支返回小的内部码，部分分支把 NTSTATUS 放在高 32 位、把失败阶段放在低 32 位。

#### 4.2.2 > 和 @ 是VT 监控请求

switch 的完整分支如下：

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/f820a9e9865ae1b9.webp)

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/dee92d23f1b47a2a.webp)

真正和 VT/SHV 初始化相关的是：

·'>'：针对指定进程地址准备大范围EPT监控请求

·'@'：针对指定进程页准备带等待时间的单页粒度EPT监控请求（更加精细化）

【而且这两个每次都是在收到指令时才开始初始化部署hypervisor，并且每一次结束的时候都会退出hypervisor，而不是驱动加载时就直接开始全局hypervisor监控直到驱动卸载!】

#### 4.2.3 从用户态消息到 EPT 和 VMCS的初始化全流程

从 sub_140065395 往下，初始化链可以整理为：

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/2375ed97a2128a13.webp)

简单来说这条链的流程可以分为四步：

·准备每个逻辑 CPU 的 VT 状态和运行所需内存。由于它需要全核虚拟化，它需要在多处理器环境中组织各 CPU 的状态、保存 VMX 相关区域，并通过 DPC 类机制完成跨核广播。  
·建立 EPT 结构。sub_1400ED134、sub_1400ECD70 一类函数负责构建或补齐 EPT 页表；之后 sub_1400EDB38、sub_1400ED6AC 会定位到目标物理页对应的 EPT 项，并按请求修改 R/W/X 权限。

·进入 VMX operation。sub_1400EBF98 能看到典型的

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/03da038cc9ad5474.webp)

这说明驱动已经进入准备运行 VMCS的阶段。

·连续 VMWRITE 配置 VMCS。sub_1400EC524 会填充 guest state、host state、控制字段和 VM-exit/VM-entry 控制项。全部准备好以后，sub_1400EDDCC 执行 VMLAUNCH。到这一刻，对指定物理页的监控就开始真正生效，在【4.3】中会讲解它如何利用这一机制进行检测。

#### 4.2.4 结束、卸载与上报

监控命中、请求完成或等待超时后，清理路径会进入 sub_1400EB204。这一部分会设置停止状态、通过跨 CPU 的 DPC 清理 VMX 相关状态、清空本次配置并释放资源。而不是初始化后永久接管所有地址访问。

最后，具体 handler 的结果被写到：

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/dde8405c7a9457b6.webp)

随后控制流返回第一层分发器，sub_1400535B5 会补充输出包的固定字段，sub_14005B5DB 再次 XOR 加密输出包，最终通过 \\PacDomain 返回用户态

### 4.3 DPC-APC-RtlWalkFrameChain回溯 + EPT物理页监控

4.2 的最后一步是 VMLAUNCH。到了这里，驱动已经把用户态请求中的目标 VA 转成物理页，建立 EPT 映射，并按请求修改该页的 R/W/X 权限。接下来只要 Guest 对这个物理页做出的访问与被关闭的权限相冲突，CPU 就会产生 EPT Violation，退出 Guest，进入驱动的 VM-exit 处理逻辑。

因此，4.2 讲的是某美的虚拟机怎么装上；4.3要关注的则是EPT Violation触发以后驱动到底收集什么、怎样把 VM-exit 上下文转成线程调用栈、最后怎样上报用户态？

那么我们这次的主角就是sub_1400EEBD0 函数，此函数负责的是 VM 已经启动以后，对一次 VM-exit 进行分类和运行检测的业务逻辑。

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/9e753551e431159f.webp)

#### 4.3.1 从 EPT Violation 快速过滤命中

sub_1400EEBD0 先读取多个 VMCS exit 字段，并取出 VM-exit reason。这里真正进入监控分支的条件可以概括为：

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/47c559b50ab7b1a0.webp)

其中 48 对应 EPT Violation；全局模式为 2 则说明当前正处在前面【4.2】 @ 命令建立的单页、带等待时间的EPT内存监控模式。

满足这些基础条件后。它会调用 sub_1400ED848，根据本次 VM-exit 的物理页信息在 EPT 目标表中查找对应项，并检查当前访问类型是否与该目标项配置的权限掩码有交集，如果有那么就继续往下走，否则就ret。

命中后，sub_1400EEBD0 会通过全局保存的回调指针调用当前模式的过滤函数。模式 2 这里对应的是 sub_1400650D8

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/16434bdb36ce40bf.webp)

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/a4df1cae771b9ab8.webp)

到后面的sub_1400650D8 还会继续验证：

· 命中地址是否等于目标地址，或至少落在同一 4KB 页

· 可选的上下文值是否匹配

· 另一项保存的元数据是否匹配

· 当前访问标志是否命中配置的访问掩码

只有全部通过，它才返回true。

#### 4.3.2 VM-exit 不直接进行栈回溯，而是复制快照投递 DPC

快速过滤返回成功后，sub_1400EEBD0 会先通过原子操作把当前模式从 2 切换为 0，避免同一轮监控重复命中。随后它分配一个约 0x8E0 字节的对象，把 VM-exit 现场相关的 0x830 字节快照复制进去，并引用当前线程对象，初始化并投递 DPC

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/8c898b39faa46cc7.webp)

这里设计其实是非常聪明的，因为vmexit应该尽快恢复guest执行，要不然耗时会太长，所以说这其实是一个值得学习的设计思路。

然后继续往下看，DPC 的 DeferredRoutine 本身也不直接调用 RtlWalkFrameChain。它会把工作继续投递成一个 APC

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/470f3e294929954e.webp)

也就是说，它抓栈回溯的整体方式是：

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/d6e73607abebc19e.webp)

这样做的意义是，最终的回溯在被命中线程所属的执行上下文中完成，可以拿到确定的调用链。

#### 4.3.3 RtlWalkFrameChain 的两次调用

继续追，真正的栈回溯发生在 APC 的 KernelRoutine，也就是 sub_1400EE950, 在开始回溯之前，它会先检查几个条件：

· 当前EPT监控是否已经被停止

· 当前请求序号是否仍然匹配

· 当前回调上下文是否仍然匹配

· 回调函数指针是否仍然存在

这些检查是为了防止 DPC/APC 排队期间请求已经超时、SHV虚拟化已卸载，或者新的请求已经覆盖旧配置时，旧 APC 还继续把过期数据上报。

满足条件后，它准备一个最多容纳 0x100 个返回地址的数组。这里并不是只调用一次 RtlWalkFrameChain，而是存在两次不同参数的调用，下面将进行解释。

第一段调用是条件性的：

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/ecfc34317b1a32c0.webp)

只有保存下来的上下文字段落在高地址范围时，才会执行这次 Flags = 0 的回溯。从调用形状看，这一段用于保留高地址上下文对应的内核侧调用链。

第二段调用则始终执行：

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/a90c2806892a9b82.webp)

它从第一段已经写入的地址之后继续填充，剩余容量为 0x100 - count0，避免两段结果互相覆盖。最后总帧数为

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/e5ca4b2eccc8c77d.webp)

也就是说，它会分成两个栈回溯分别查内核态和用户态的，最后再拼到一起得到完整的调用栈。

#### 4.3.4 记录和KEVENT通知阶段

两段栈帧收集拼接完成后，sub_1400EE950 再次调用和 VM-exit 阶段相同的回调指针：

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/8609eae52b2469d3.webp)

此时最后一个参数不再是 0，而是实际回溯到的帧数。对于模式 2 的 sub_1400650D8，这会进入真正的记录路径

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/e704a7a56b05368e.webp)

伪c代码如下：

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/6ab56f8cf251da8a.webp)

#### 4.3.5 上报给用户态结果

前面 @ 命令进入的 worker 是 sub_1400652A0。它在安装 SHV 后，会把本次线程优先级提高，并按用户态提供的超时时间等待该 KEVENT

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/67e6c8949a74aaff.webp)

然后最后就走【4.2】里的上报链路返回给用户态结果，至此用户态得到了对关注的内存页进行违规访问程序的完整栈回溯。

![](⚠️ https://bbs.kanxue.com/src) ![](https://cdn.jsdelivr.net/gh/zhiyu-zeng/img@main/img/2026/07/02e24191743b4dcc.webp)

#### 4.3.6 实际场景中对cheat会有什么不同的挑战

从行为角度看，下面三类访问都有可能进入这条链，但前提都是：访问的确落在用户态本次提交、驱动实际纳入 EPT 控制的物理页上，并且访问类型和后续过滤条件都匹配。

·第一种是外部用户态程序读写cs2进程。例如外部程序通过 ReadProcessMemory 间接触发内核对目标物理页的读取。如果该页刚好被关掉 EPT Read 权限，且上下文满足条件，就可能发生 EPT Violation。之后记录中大概率会同时出现内核复制路径和对应线程的用户态调用链

·第二种是内核驱动附加cs2进程后直接读写。此时命中的 RIP 可能落在该驱动自身代码、内核复制函数或其他内核例程中。DPC/APC 阶段收集到的两段栈帧，大概率可以把访问归属到具体内核路径，从而检测kdm驱动之类的

·第三种是dll注入直接指针访问。即使代码运行在目标进程地址空间，只要它访问的是被 EPT 权限控制的物理页，仍然会产生 EPT Violation。此时回溯结果更可能包含目标进程内模块、用户态函数和相关调用链，很大概率可以获得异常内存页面或者无模块信息的地址

[#调试逆向](https://bbs.kanxue.com/forum-4-1-1.htm) [#系统底层](https://bbs.kanxue.com/forum-4-1-2.htm) [#加密算法](https://bbs.kanxue.com/forum-4-1-5.htm)
